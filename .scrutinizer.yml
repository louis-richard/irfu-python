checks:
    python:
        code_rating: true
        duplicate_code: true

build:
  nodes:
    analysis:
      tests:
        override:
          - py-scrutinizer-run
          - pylint-run ./pylintrc
filter:
    excluded_paths:
        - tests/*